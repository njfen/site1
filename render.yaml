services:
  - type: web
    name: peptide-guide
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && python web/export_static.py
    startCommand: gunicorn --chdir web server:app --bind 0.0.0.0:$PORT
    healthCheckPath: /
